[project]
name = "typeless-local"
version = "0.2.0"
requires-python = ">=3.12"

[project.optional-dependencies]
build = ["py2app>=0.28"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
filterwarnings = [
  "error::DeprecationWarning",
]
